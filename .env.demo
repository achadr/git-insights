# ================================
# DEMO MODE ENVIRONMENT VARIABLES
# ================================
#
# This file shows the minimal configuration needed for
# demo deployments without Anthropic API costs
#
# Copy this to .env in backend and frontend directories
# for demo/portfolio deployments

# ================================
# Backend Configuration
# ================================

# CRITICAL: Set to development for demo mode
NODE_ENV=development

# Server port
PORT=3000

# GitHub token (still required to fetch repository data)
# Get from: https://github.com/settings/tokens
# Required scopes: repo, read:user
GITHUB_TOKEN=your-github-token-here

# Frontend URL (update after deployment)
ALLOWED_ORIGINS=http://localhost:5174,http://localhost:5174,https://your-frontend.vercel.app

# Optional: Rate limiting
RATE_LIMIT_FREE_TIER=10
RATE_LIMIT_WINDOW_MS=86400000

# ================================
# Frontend Configuration
# ================================

# Backend API URL (update after backend deployment)
VITE_API_URL=http://localhost:3000

# ================================
# Notes
# ================================
#
# With NODE_ENV=development:
# - No Anthropic API key needed ✓
# - No API costs ($0) ✓
# - Mock analysis data returned ✓
# - Real GitHub data still fetched ✓
# - Perfect for portfolio demos ✓
#
# NOT NEEDED in demo mode:
# - ANTHROPIC_API_KEY (will use mock data)
# - REDIS_URL (will use in-memory storage)
